syntax = "proto3";

import "common.proto";

package Spy.Recipe;

message RequestCreate {
    int32 recipe_id = 1;
    int32 boost_item_id = 2;
    bool is_exchange_crystal = 3;
}

message ResponseCreate {
    Common.UpdateResourceResult	update_resource_result = 1;
}